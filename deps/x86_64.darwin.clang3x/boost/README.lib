This is the directory into which you should symlink your 64 bit boost build artifacts..
You need to build them using clang 3.1 or higher.
Note that since the makefile specifies the .a files explicitly they will be statically linked.
